"use client";
import { useEffect, useMemo, useState } from "react";
import { Check, ChevronRight, LoaderCircle, Save, Send } from "lucide-react";
import { controlTestDefinitions } from "@/lib/control-test";
type Athlete={id:string;name:string;grade:string|null;event:string|null};type Session={id:string;title:string;measured_on:string;selected_test_codes:string[];scan_teams:{name:string}|null};type Values=Record<string,string[]>;
const key=(code:string)=>`vaultex-scan-student-${code}`;
export default function StudentEntryClient({joinCode,session,athletes}:{joinCode:string;session:Session;athletes:Athlete[]}){const tests=controlTestDefinitions.filter(item=>session.selected_test_codes.includes(item.code));const [athleteId,setAthleteId]=useState("");const [pin,setPin]=useState("");const [token,setToken]=useState("");const [values,setValues]=useState<Values>({});const [step,setStep]=useState(0);const [saving,setSaving]=useState(false);const [message,setMessage]=useState("");const [submitted,setSubmitted]=useState(false);const current=tests[step];const athlete=athletes.find(item=>item.id===athleteId);
 useEffect(()=>{const timer=window.setTimeout(()=>{try{const raw=localStorage.getItem(key(joinCode));if(raw){const saved=JSON.parse(raw);setAthleteId(saved.athleteId??"");setToken(saved.token??"");setValues(saved.values??{});}}catch{}},0);return()=>window.clearTimeout(timer)},[joinCode]);
 useEffect(()=>{if(token)localStorage.setItem(key(joinCode),JSON.stringify({athleteId,token,values}))},[athleteId,joinCode,token,values]);
 const completed=useMemo(()=>tests.filter(item=>values[item.code]?.some(Boolean)).length,[tests,values]);
 async function enter(){if(!athleteId||!/^\d{4}$/.test(pin)){setMessage("自分の名前と4桁の暗証番号を入力してください。");return}setSaving(true);setMessage("");try{const response=await fetch(`/api/scan/join/${joinCode}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"claim",athleteId,pin})});const result=await response.json();if(!response.ok)throw new Error(result.error);setToken(result.token);setValues(result.values??{});setSubmitted(result.status!=="inputting");}catch(error){setMessage(error instanceof Error?error.message:"開始できませんでした。")}finally{setSaving(false)}}
 async function persist(action:"save"|"submit"){if(!token)return;setSaving(true);setMessage("");try{const response=await fetch(`/api/scan/join/${joinCode}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action,athleteId,token,values})});const result=await response.json();if(!response.ok)throw new Error(result.error);if(action==="submit"){setSubmitted(true);localStorage.removeItem(key(joinCode));setMessage("提出しました。顧問の画面へ反映されています。");}else setMessage("途中保存しました。");}catch(error){setMessage(error instanceof Error?error.message:"保存できませんでした。")}finally{setSaving(false)}}
 if(!token)return <section className="mt-8 rounded-3xl border border-white/10 bg-[#101311] p-5 sm:p-7"><h2 className="text-2xl font-black">自分の名前を選択</h2><p className="mt-2 text-sm text-white/45">初回は自分で決めた4桁の暗証番号を設定します。再開するときにも使います。</p><label className="mt-6 block text-xs font-bold text-white/45">選手名<select value={athleteId} onChange={event=>setAthleteId(event.target.value)} className="mt-2 w-full rounded-xl border border-white/15 bg-[#0b0d0c] px-4 py-4 text-white"><option value="">自分の名前を選ぶ</option>{athletes.map(item=><option key={item.id} value={item.id}>{item.name}{item.grade?`（${item.grade}）`:""}</option>)}</select></label><label className="mt-4 block text-xs font-bold text-white/45">4桁の暗証番号<input inputMode="numeric" pattern="[0-9]*" maxLength={4} value={pin} onChange={event=>setPin(event.target.value.replace(/\D/g,"").slice(0,4))} placeholder="0000" className="mt-2 w-full rounded-xl border border-white/15 bg-black/30 px-4 py-4 text-center text-3xl font-black tracking-[.45em] text-white"/></label><button onClick={enter} disabled={saving} className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-orange-500 px-5 py-4 font-black text-black disabled:opacity-40">{saving?<LoaderCircle className="animate-spin" size={18}/>:<ChevronRight size={18}/>}入力を始める</button>{message?<p className="mt-3 text-sm text-red-300">{message}</p>:null}</section>;
 if(submitted)return <section className="mt-8 rounded-3xl border border-emerald-400/30 bg-emerald-400/10 p-8 text-center"><Check className="mx-auto text-emerald-300" size={40}/><h2 className="mt-4 text-2xl font-black">提出完了</h2><p className="mt-2 text-sm text-white/55">{athlete?.name}さんの記録を顧問へ送りました。</p>{message?<p className="mt-4 text-sm text-emerald-200">{message}</p>:null}</section>;
 const attempts=values[current.code]??["",""];const numbers=attempts.map(Number).filter(item=>item>0);const best=numbers.length?(current.betterDirection==="lower"?Math.min(...numbers):Math.max(...numbers)):null;
 return <div className="mt-8"><div className="flex items-center justify-between text-xs font-bold"><span>{athlete?.name}</span><span className="text-emerald-300">{completed}/{tests.length}種目入力</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-white/10"><div className="h-full bg-emerald-400 transition-all" style={{width:`${tests.length?completed/tests.length*100:0}%`}}/></div><nav className="mt-5 flex gap-2 overflow-x-auto pb-2">{tests.map((item,index)=><button key={item.code} onClick={()=>setStep(index)} className={`shrink-0 rounded-full border px-4 py-2 text-xs font-black ${index===step?"border-orange-500 bg-orange-500 text-black":values[item.code]?.some(Boolean)?"border-emerald-400/40 text-emerald-300":"border-white/15 text-white/45"}`}>{item.category}</button>)}</nav><section className="mt-4 rounded-3xl border border-orange-500/25 bg-[#101311] p-5"><p className="text-xs font-black tracking-[.16em] text-orange-400">{current.abilityEn}</p><h2 className="mt-1 text-3xl font-black">{current.category}{current.code==="speed_endurance_300m"?"（150m・300m）":""}</h2><div className="mt-6 grid grid-cols-2 gap-3">{attempts.map((attempt,index)=><label key={index} className="text-xs font-bold text-white/40">{index+1}本目<input inputMode="decimal" value={attempt} onChange={event=>setValues(existing=>({...existing,[current.code]:attempts.map((value,i)=>i===index?event.target.value:value)}))} placeholder="記録" className="mt-2 w-full rounded-xl border border-white/15 bg-black/30 px-3 py-4 text-center text-2xl font-black text-white outline-none focus:border-orange-500"/></label>)}</div><div className="mt-4 rounded-xl bg-orange-500/10 p-4 text-center text-sm text-white/45">採用記録 <strong className="ml-2 text-2xl text-orange-300">{best??"—"}</strong> {current.unit}</div><div className="mt-5 flex gap-2"><button disabled={step===0} onClick={()=>setStep(value=>Math.max(0,value-1))} className="flex-1 rounded-xl border border-white/15 px-4 py-3 text-sm font-bold disabled:opacity-25">前へ</button><button disabled={step===tests.length-1} onClick={()=>setStep(value=>Math.min(tests.length-1,value+1))} className="flex-1 rounded-xl border border-white/15 px-4 py-3 text-sm font-bold disabled:opacity-25">次へ</button></div></section><div className="sticky bottom-3 mt-5 grid grid-cols-2 gap-3 rounded-2xl border border-white/10 bg-[#101311]/95 p-3 backdrop-blur"><button disabled={saving} onClick={()=>persist("save")} className="inline-flex items-center justify-center gap-2 rounded-xl border border-emerald-400/30 px-4 py-3 text-sm font-black text-emerald-300"><Save size={16}/>途中保存</button><button disabled={saving||!completed} onClick={()=>{if(window.confirm(`入力した${completed}種目を提出しますか？\n提出後は変更できません。`))void persist("submit")}} className="inline-flex items-center justify-center gap-2 rounded-xl bg-orange-500 px-4 py-3 text-sm font-black text-black disabled:opacity-40">{saving?<LoaderCircle className="animate-spin" size={16}/>:<Send size={16}/>}提出</button></div>{message?<p className="mt-3 text-center text-sm text-emerald-300">{message}</p>:null}</div>}
